import 'package:flutter/material.dart';

class CustomDescription{
  static const primaryShadow = BoxShadow(color: Colors.black, blurRadius: 3, offset: Offset(2, 3));
  static var primaryBorderRadius = BorderRadius.circular(8);
  static const primaryEdgeInsets = EdgeInsets.all(8);
}